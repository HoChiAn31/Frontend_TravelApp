import { View } from "react-native";
import React from "react";

const HeightLine = ({ height }) => {
  return <View style={{ height: height, backgroundColor: "#fff" }}></View>;
};

export default HeightLine;
